<Window x:Class="FlexiSuiteAPP.App.Views.Popups.CustomPopup"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="CustomPopup"
        WindowStyle="None"
        AllowsTransparency="False"
        Background="Transparent"
        Topmost="True"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Height="250"
        Width="350">
    <Grid>
        <Border 
            Background="White" 
            Padding="10" 
            BorderThickness="1"
            BorderBrush="Black">
            <TextBlock Text="This is a custom popup"
                       HorizontalAlignment="Center" VerticalAlignment="Center" />
        </Border>
    </Grid>
</Window>
